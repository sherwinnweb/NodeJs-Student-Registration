<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>handlebars example</title>
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h3>
            {{#if title}}
            {{title}}
            {{/if}}
        </h3>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">First Name</th>
                    <th scope="col">Last Name</th>
                    <th scope="col">Email</th>
                    <th scope="col">Course</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                {{#each data}}
                <tr>
                    <td> {{this.student_id}}</td>
                    <td> {{this.first_name}}</td>
                    <td> {{this.last_name}}</td>
                    <td> {{this.email}}</td>
                    <td> {{this.course_name}}</td>
                    <td> <a href="/auth/updateform/{{this.email}}" class="btn btn-primary">Edit</a><a href="/auth/deleteStudent/{{this.email}}" class="btn btn-danger" onClick="return confirm('Delete')">Delete</a></td>
                    
                <tr>
                {{/each}}
                </tr>
            </tbody>
        </table>
        <a href="/auth/logout" class="btn btn-info" onClick="return confirm('Are you sure?')">Log out</a>
        <a href="/register" class="btn btn-info" onClick="return confirm('Are you sure?')">Add Student</a>
    </div>
        
    

    
    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
    {{!-- <script>
        let a;
        function myFunction(t) {
        if (confirm("Press a button!") == true) {
            t.href.setAttribute("href","/auth/deleteUser/{{this.email}}")
        } else {
            t.target.setAttribute("href","/auth/accounts")
        }
        }

    </script> --}}
</body>
</html>